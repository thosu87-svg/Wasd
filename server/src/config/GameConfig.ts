export const GameConfig = {
  chunkSize: 64,
  tickRateMs: 100,
  defaultPort: 3000,
  maxObserverRadius: 6,
  matrixCurrency: "matrix_energy",
  interactDistance: 25,
  attackDistance: 35
} as const;